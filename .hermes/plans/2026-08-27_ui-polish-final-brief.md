# FINAL IMPLEMENTATION BRIEF — UI Polish v2 ("2030 Mission Briefing Room")
**Repo:** /opt/data/ai-lab-tycoon, branch master. **Sources synthesized:** investigator.md, adversary.md, alternative.md, design-audit.md, luna-third-review.md (all in /opt/data/cache/ui-triage/). Where sources conflicted, the reconciliation below is the single source of truth.

## Verified ground rules (from the trio)
- Delta snapshots key on run `revision`, NEVER `state.meta.week` (mid-week commands change resources without changing week).
- Research NODES have no progress field — Progress bars are for PROJECTS (progress/duration) and RIVALS (0-100 ints) only. No research-node bars.
- Toast policy per call-site; PriorityStrip advance goes through executeEngineCommand but the header AdvanceWeekButton does NOT (advance-week-button.tsx:44) — never wire toasts into executeEngineCommand; fire discrete-command toasts from the named wrapper functions.
- No `state.history` exists — all week-over-week math is client-side derived.
- localStorage reads must be useEffect-only (prerendered / and /play hydrate from static HTML).
- `game-shell.tsx` is unreferenced dead code — DELETE it (audit verdict: the third duplicate header implementation).
- Carry `aria-labelledby` ids through restructures; no tests catch dangling ones.

## COMMIT SERIES (execute in order, each gated on: cd apps/web && pnpm build, then NODE_OPTIONS=--max-old-space-size=6144 tsc -p apps/web/tsconfig.json --noEmit, biome --write touched, cd packages/engine && ../../node_modules/.bin/vitest run 31 files/271 green. Run web vitest too after commits 2 and 4.)

### COMMIT 1 — 'refactor(web): derived layer, context split, layout de-duplication'
Foundation, no visual redesign:
1. New apps/web/src/game/derived/week-digest.ts: pure functions diffing prev vs current GameState into a typed WeekDigest (launches, training completions, evaluations, incidents, funding events, project completions) sourced from state.reports.items filtered to fact.week === newWeek. Numeric resource deltas via selectResourceBar(prev) vs selectResourceBar(next).
2. New apps/web/src/game/derived/use-week-digest.ts: keeps prev state in a ref keyed on run revision (NOT meta.week); 2-entry ring buffer (enables rival velocity later); returns {digest, deltas}.
3. Context split in game-state-context.tsx: UiStateContext (acknowledgedReportIds, milestoneDismissed, density, digest dismissal) vs RunContext (session/save/controller). All useGameState() import sites updated; acknowledged-report writes move to the UiState slice (fixes live bug: acknowledging a report re-renders the 635-line LabCore).
4. useMemo on selectVisibleState/selectTeams calls in OverviewPanel + AdvanceWeekButton (reference-equality cache key is valid: engine returns fresh graphs per command).
5. DELETE apps/web/src/game/components/game-shell.tsx (773 lines, unreferenced by routes; verify no test imports it first, update any that do).
6. Dedupe week display: remove the standalone 'Current week' card in game.tsx:277-287 (ResourceBar Time cell is the single source).

### COMMIT 2 — 'feat(web): week digest, resource intelligence, header focus'
The semantic pass (digest card, delta chips, progress, header — all touch game.tsx):
1. New week-digest-card.tsx: shadcn Accordion under AdvanceWeekButton; collapsed one-line summary ('Week 14: Apex-1 launched · incident contained · +$800'), expandable rows with icons + link to the acting route; aria-live upgrades from report count to the digest summary line.
2. resource-bar.tsx: delta chips per resource from use-week-digest deltas (+N green / -N red / hidden at 0, 2s fade, aria-hidden); add title tooltips.
3. Progress bars: team-panel + model-card training (project.progress/duration — NOTE integer weeks: render as Math.round fraction), rivals-panel (progress/100). NO research-node bars (nodes are status-only). Use ProgressLabel/ProgressValue subcomponents per Base UI API.
4. run-menu.tsx: DropdownMenu (New run / Delete run via shadcn Dialog confirm replacing BOTH window.confirm sites: game-state-context.tsx:281-289 and start-run-form.tsx:69-76 / divider / disabled 'Keyboard shortcuts' item — reword to remove internal 'Wave 2' language). Replace the Avatar-with-MoreHorizontal trigger with an outline Settings2 icon button. Route 'New run' DIRECTLY to /play with explicit new-run intent (carry ?new=1 search param consumed by play.tsx to set screen='new' — the current chooseNewRun() + navigate('/') detour loses state and lands on Resume).
5. Header slimming per design-audit X1/H1/H2: collapse game.tsx double header into ONE slim top bar (company name, EraBadge, SessionBadge, run-menu); GamePage (game-page.tsx) owns the page title; remove tagline + 'V1 sandbox' label from active-run screens; remove all 12 'Module NN /' + 'Archive /' kicker eyebrows (6 Module in routes/game.*.tsx:35/28/47/32/38/29 + 6 Archive in lineage:28/chronicle:30/agiprogram:30/notebook:30/quarterly:30/pulse:30). Carry heading ids through restructures.
6. Landing/play fixes from design-audit + luna: /play skip-link target (add id="main-content" to the selection surface main/section in play.tsx); mobile art dead-zone on play (show one tinted backdrop below sm); delete button out of the primary row on play (overflow/confirm pattern); hero md-breakpoint empty track fix (compact premise variant at sm); single section-level Preview note on landing archive strip instead of 4 per-card badges; landing main gets game-shell class OR move focus-visible rules to :root so focus rings match.

### COMMIT 3 — 'fix(web): a11y, contrast, responsive hardening'
Accessibility + theme + responsive (from luna-third-review, verified findings):
1. Contrast pass (luna matrix: light-mode primary/bg = 4.36/4.44/3.92/4.24 — all below 4.5 AA; amber 2.38-3.07, positive 3.12-3.27, negative 3.58-4.16): darken/adjust light-mode --primary and the four game status colors per motif to reach >=4.5 normal-text; verify default button primary-foreground pair in solarflare-light (3.92) and deepfreeze-light (4.42). Show the recomputed matrix in the commit body.
2. Token hygiene: .surface-card ring-white/5 + shadow-black/20 -> theme-variable equivalents; replace hardcoded cyan-300/rgba(34,211,238) accents (start-run-form.tsx:174-177, rivals-panel.tsx:87, incident-card.tsx:224, report-queue.tsx:124, run-result.tsx:50) and amber shadow rgba(245,176,76,.1) (lab-notebook.tsx:239) with tokens; standardize pane.tsx:70 amber spelling to var(--game-amber); retire art-v1 advisor-ops.png in run-result.tsx:21 -> an art-v2 advisor treatment; fix dialog.tsx:31 bg-black/10 and art-frame.tsx:52 fixed white/black shadows to token-safe values.
3. Terminal/lost-run UX (luna HIGH): global terminal handling in game.tsx layout — when state.terminal.status === 'lost', render RunResult (or a global result banner) at layout level with role=alert + focus handoff, replace PriorityStrip copy with restart guidance, keep dimmed LabCore; no longer bury RunResult only in products page below panels.
4. Decision overlay busy/live: pass game.actionBusy into DecisionOverlay -> disabled on all choice buttons while resolving + aria-busy on the dialog + 'Resolving decision…' announcement; remove hardcoded disabled={false} (game.tsx:862-888).
5. Focus contracts: route-change focus handoff to page heading in game.tsx; decision close returns focus to opener; research detail pane gets role=dialog + aria-modal + escape + focus transfer (or Sheet) at mobile; repair advance-week-button focus helper (currently unreachable-while-disabled and targets legacy decision-card-* ids that only exist in deleted game-shell) to target the active pane/decision.
6. Responsive/touch: LabCore — keyboard operable or decorative (choose decorative: drop drag-rotate claims, remove touch-none so mobile page scroll works, keep subtle idle animation); enforce 44px floor for anchors and portaled controls (extend globals.css rule + fix h-7/size-7/sm sizes in priority-strip.tsx:133-145, game.tsx nav anchors, dialog/ticker/theme-switcher controls); safe-area-aware shared bottom offset for news-ticker + mobile nav + content scroll-padding (fixes ~15px ticker overlap); AGI vault narrow-layout socket clipping fix (responsive positions or min padding — luna math: left socket edge -13.9px at 390px); theme-provider.tsx:127-129 move window.localStorage access inside the try; ThemeSwitcher non-landing overlap — reserve header row space or put it in flow; Chronicle/Quarterly prose measure constrained to ~70ch; ticker link accessible name includes current headline (aria-label currently overrides descendants); news-ticker headline removal only via its own toggle; 'New run' menu item + 'd' shortcut guards (skip input/textarea/contentEditable; skip when document.querySelector('[role="dialog"]') exists).

### COMMIT 4 (optional, only if time) — 'feat(web): detail density layer'
data-density attribute on shell root + .density-detail CSS rule (zero re-render for 6/8 surfaces) + useDensity() for research-tree/overview data changes + 8-surface mapping from alternative.md section 4. SKIP if the session is running long; Concise/Detailed can ship in a later pass — commits 1-3 stand alone.

## REPORT FORMAT per commit: files changed, findings addressed (by id), verification outputs. FINAL: summary of all five review sources' findings addressed vs deferred.
